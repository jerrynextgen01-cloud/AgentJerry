name: TEST – Scheduler Every 2 Minutes

on:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduler test
        run: |
          echo "Automatic schedule is running"
          date -u
          TZ=Asia/Kolkata date
